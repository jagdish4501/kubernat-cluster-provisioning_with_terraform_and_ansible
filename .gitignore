# Ignore all Terraform state files and backup files
*.tfstate
*.tfstate.*

# Ignore Terraform plan files
*.tfplan

# Ignore .terraform directory which contains plugin and provider information
.terraform/

# Ignore crash logs from Terraform
crash.log

# Ignore override files. These are user-specific and should not be versioned
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore files containing sensitive credentials (optional, if needed)
# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version 
# control as they are data points which are potentially sensitive and subject 
# to change depending on the environment.

*.tfvars
*.tfvars.json

# Ignore files used for sensitive data (e.g., secret values)
secrets.tfvars
secrets.tfvars.json

# Ignore terraform lock files (if you don't want to track version locking)
.terraform.lock.hcl

# Ensure that files ignored in the parent directory also affect subdirectories
**/*.tfstate
**/*.tfstate.*
**/*.tfplan
**/.terraform/
**/crash.log
**/override.tf
**/override.tf.json
**/*_override.tf
**/*_override.tf.json
**/*.tfvars
**/*.tfvars.json
**/secrets.tfvars
**/secrets.tfvars.json
**/.terraform.lock.hcl
